#include <stdio.h>

int add(int a, int b)
{
    return a + b;
}

int test()
{
    assert(multiply(2, 2) == 4);
    assert(multiply(-3, 2) == 6);
}

int multiply(int a, int b)
{
    return a * b;
}

int main()
{
    printf("Hi,Little Ross!");
    test();
    return 0;
}
